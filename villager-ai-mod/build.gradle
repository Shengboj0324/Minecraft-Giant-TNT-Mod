// Villager AI Mod specific configuration
archivesBaseName = 'villageraimod'

minecraft {
    runs {
        client {
            mods {
                villageraimod {
                    source sourceSets.main
                }
            }
        }

        server {
            mods {
                villageraimod {
                    source sourceSets.main
                }
            }
        }

        gameTestServer {
            mods {
                villageraimod {
                    source sourceSets.main
                }
            }
        }

        data {
            args '--mod', 'villageraimod', '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
            
            mods {
                villageraimod {
                    source sourceSets.main
                }
            }
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

// Villager AI Mod specific manifest
jar {
    manifest {
        attributes([
                "Specification-Title"     : "Villager AI Mod",
                "Specification-Vendor"   : "YourName",
                "Specification-Version"  : "1",
                "Implementation-Title"   : project.name,
                "Implementation-Version" : project.jar.archiveVersion,
                "Implementation-Vendor"  : "YourName",
                "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
} 