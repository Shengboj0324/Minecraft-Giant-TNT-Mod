// Giant TNT Mod specific configuration
archivesBaseName = 'gianttntmod'

minecraft {
    runs {
        client {
            mods {
                gianttntmod {
                    source sourceSets.main
                }
            }
        }

        server {
            mods {
                gianttntmod {
                    source sourceSets.main
                }
            }
        }

        gameTestServer {
            mods {
                gianttntmod {
                    source sourceSets.main
                }
            }
        }

        data {
            args '--mod', 'gianttntmod', '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
            
            mods {
                gianttntmod {
                    source sourceSets.main
                }
            }
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

// Giant TNT Mod specific manifest
jar {
    manifest {
        attributes([
                "Specification-Title"     : "Giant TNT Mod",
                "Specification-Vendor"   : "Shengbo Jiang",
                "Specification-Version"  : "1",
                "Implementation-Title"   : project.name,
                "Implementation-Version" : project.jar.archiveVersion,
                "Implementation-Vendor"  : "Shengbo Jiang",
                "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
} 